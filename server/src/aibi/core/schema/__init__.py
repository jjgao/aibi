"""Pydantic models: the single definition of every descriptor, document and result shape.

JSON Schema, OpenAPI, MCP tool schemas and TypeScript types are generated from these models
(SPEC §12.4). Nothing in this package knows about any domain (SPEC P8).
"""
